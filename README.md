# El analizador inteligente de datos simples.

Este proyecto permite analizar datos de un csv. Se tiene un backend rápido y eficiente con librerías de python
para análisis numérico (python, numpy, etc) con un API utilizando grpc, y una pagina web hecha con svelte.
